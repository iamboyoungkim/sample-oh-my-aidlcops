{
  "$id": "https://aws-samples.github.io/sample-oh-my-aidlcops/schemas/ontology/deployment.schema.json",
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Deployment",
  "description": "A deployable unit produced by AIDLC Construction and consumed by AgenticOps. Represents the handoff artifact between Phase 2 and Phase 3.",
  "type": "object",
  "additionalProperties": false,
  "required": ["id", "target", "artifact", "approval_state"],
  "properties": {
    "id": {
      "type": "string",
      "pattern": "^[a-z][a-z0-9-]*$",
      "maxLength": 64
    },
    "target": {
      "description": "Where this deployment runs.",
      "enum": ["eks", "ec2", "lambda", "ecs", "bedrock-agentcore", "sagemaker"]
    },
    "artifact": {
      "description": "Primary build output. Legacy string form kept for backward compatibility; the object form carries SLSA v1.1 provenance.",
      "oneOf": [
        {
          "type": "string",
          "minLength": 1
        },
        {
          "type": "object",
          "additionalProperties": false,
          "required": ["uri", "digest"],
          "properties": {
            "uri": {
              "description": "Canonical artifact URI (image ref, OCI digest URL, S3 URI).",
              "type": "string",
              "minLength": 1
            },
            "digest": {
              "description": "SHA-256 digest in the form sha256:<64 hex chars>.",
              "type": "string",
              "pattern": "^sha256:[a-f0-9]{64}$"
            },
            "provenance_uri": {
              "description": "URI of the SLSA v1.1 JSON provenance document.",
              "type": "string",
              "format": "uri"
            },
            "signing": {
              "type": "object",
              "additionalProperties": false,
              "properties": {
                "cosign_bundle_uri": {
                  "type": "string",
                  "format": "uri"
                },
                "issuer": {
                  "type": "string",
                  "maxLength": 256
                }
              }
            },
            "builder": {
              "description": "Builder identifier (github-actions, buildkite, etc).",
              "type": "string",
              "maxLength": 64
            }
          }
        }
      ]
    },
    "manifests": {
      "description": "Paths to IaC / Helm / k8s manifest files that describe this deployment.",
      "type": "array",
      "items": { "type": "string" }
    },
    "approval_state": {
      "description": "Human-in-the-loop checkpoint state. AgenticOps only progresses on 'approved'.",
      "$ref": "../common/enums.schema.json#/$defs/approvalState"
    },
    "rollback_plan": {
      "description": "Path or inline description of how to roll back. Required before approval_state=approved in strict mode.",
      "type": "string"
    },
    "blast_radius": {
      "description": "What breaks if this deployment misbehaves. Used by incident-response skill.",
      "$ref": "../common/enums.schema.json#/$defs/blastRadius"
    },
    "produced_by": {
      "description": "Agent or skill id that generated this deployment.",
      "type": "string"
    },
    "spec_ref": {
      "description": "Pointer to the Spec artifact this deployment satisfies (.omao/plans/...).",
      "type": "string"
    },
    "adr_refs": {
      "description": "ADR identifiers that constrain this deployment.",
      "type": "array",
      "items": { "type": "string" }
    },
    "approval_chain": {
      "description": "Optional ordered list of approvals gating approval_state=approved. Under --strict-enterprise this array must be non-empty when approval_state=approved.",
      "$ref": "../common/approval-chain.schema.json#/$defs/approvalChain"
    },
    "risk_exceptions": {
      "description": "Risk.id values whose gate_ref was explicitly waived for this deployment. Closes the Risk -> Deployment traceability gap.",
      "type": "array",
      "items": { "type": "string", "pattern": "^[a-z0-9][a-z0-9-]*$" },
      "uniqueItems": true
    }
  }
}
